export const queries = `

`;

export const mutations = `

`;

export const types = `
  type UserSlim {
    firstName: String!
    lastName: String!
    tag: String!
    image: String
  }

  type User {
    firstName: String!
    lastName: String!
    email: String!
    image: String
    mobile: String!
    tag: String!
  }
`;
